#include "main.h"

/**
 * _isupper - checks character if is uppercase or not.
 * @c: character to check.
 * Return: returns 1 if isupper and 0 if otherwise.
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
