#include "main.h"

/**
 * _isdigit - checks character if is digit or not.
 * @c: character to check.
 * Return: returns 1 if isdigit and 0 if otherwise.
 */

int _isdigit(int c)
{
	return (c >= 48 && c <= 57);
}
